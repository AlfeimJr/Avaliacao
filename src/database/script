CREATE DATABASE traffic;

use traffic;

CREATE TABLE traffic.users (
  id int(10) PRIMARY KEY auto_increment,
  name varchar(200),
  email varchar(200),
  message varchar(200),
  create_at TIMESTAMP  
);